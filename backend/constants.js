const MAX_PLAYERS = 20;
const FRAME_RATE = 20;
const GRID_SIZE = 20;

const POWERUPS = ["EAT", "SPEED", "SHOOT", "GHOST"];
const WEAKNESSES = ["GHOST", "BOMB"];

module.exports = {
  MAX_PLAYERS,
  FRAME_RATE,
  GRID_SIZE,
  POWERUPS,
};
